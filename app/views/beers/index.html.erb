<div class="otsikko">
  <h1>Beers</h1>
  <p>Number of beers: <%= @beers.count %></p>
  <%= link_to "Add new beer", new_beer_path %>
</div>

<p style="color: green"><%= notice %></p>

<div id="beers">
  <% @beers.each do |beer| %>
    <%= render beer %>
  <% end %>
</div>
