<%= form_with(model: beer) do |form| %>
  <% if beer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
        <% beer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class:"form-label" %>
    <%= form.text_field :name, class:"form-control" %>
  </div>
<!--
  <div>
    <%= form.label :style, style: "display: block" %>
    <%= form.select :style, options_for_select(@styles) %>
  </div>
-->
  <div>
    <%= form.label :style_id, class:"form-label" %>
    <%= form.select :style_id, options_from_collection_for_select(@styles, :id, :text), {}, { :class => "form-select" } %>
  </div>

  <div>
    <%= form.label :brewery_id, class:"form-label" %>
    <%= form.select :brewery_id, options_from_collection_for_select(@breweries, :id, :name), {}, { :class => "form-select" } %>
  </div>

  <div>
    <button type="submit" class="btn btn-primary">Create Beer</button>
  </div>
<% end %>
